def setup():
    size(600, 400)
    global x
    global y
    y = 2
    x = 1
def draw():
    global x
    global y
    background(100)
    fill(floor(random(0,255)) , floor(random(0,255)), floor(random(0,255)))
    ellipse(x, y, floor(random(0,50)), floor(random(0,50)))
    x = x + 6
    y = y + 4
